# Crawler (BeautifulSoup uses Python's built-in html.parser — no lxml/compiler needed)
selenium==4.27.1
beautifulsoup4==4.12.3

# Database. dnspython is required to resolve Atlas mongodb+srv:// URIs.
pymongo==4.10.1
dnspython==2.8.0

# Load MONGO_URI (and friends) from a local .env file
python-dotenv==1.0.1

# Timezone data (zoneinfo needs this on Windows; Linux uses the system tzdb)
tzdata==2024.2

# Web app (API + front-end)
Flask==3.1.0
gunicorn==23.0.0
